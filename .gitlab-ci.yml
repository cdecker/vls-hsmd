cache:
  paths:
    - .cache

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  CARGO_HOME: "$CI_PROJECT_DIR/.cache/cargo"
  SUBDAEMON:
    value: "hsmd:remote_hsmd_vls"
    description: "use the single-binary hsmd replacement (default), or the gRPC version `hsmd:remote_hsmd` or the 2nd gRPC version: `hsmd:remote_hsmd_grpc2`"
  LIGHTNING_REF:
    value: ""
    description: "rebase onto the ref (e.g. master) from the upstream c-lightning repository"

stages:
  - vls
  - test

compile-vls:
  stage: vls
  image: rust:1.58.1
  timeout: 30m
  script:
    # we don't need to rebase lightning here, but fail fast if rebase fails
    - ./scripts/maybe-rebase "$LIGHTNING_REF"
    # latest rust images seem to require rustfmt for cargo build
    - rustup component add rustfmt
    - (cd vls && cargo build --locked --bins)
    - mkdir -p bin
    - cp vls/target/debug/vlsd bin/
    - cp vls/target/debug/remote_hsmd_{vls,vls_grpc2} bin/
    - ls -l bin
  artifacts:
    paths:
      - bin/vlsd
      - bin/remote_hsmd_vls
      - bin/remote_hsmd_vls_grpc2
      - bin/remote_hsmd_vls_embedded

test-inplace:
  stage: test
  image: ubuntu
  script:
    - ls -l
    - ls -l bin
    - ./scripts/install-additional
    - python3 --version
    - ./scripts/maybe-rebase "$LIGHTNING_REF"
    - ./scripts/setup-remote-hsmd
    - source scripts/setup-env
    - cd lightning
    - bash -ex .github/scripts/setup.sh  # missing `set -e` internally
    - bash -ex .github/scripts/build.sh  # missing `set -e` internally

test-experimental:
  stage: test
  image: ubuntu
  variables:
    EXPERIMENTAL_FEATURES: "1"
  script:
    - ./scripts/install-additional
    - ./scripts/maybe-rebase "$LIGHTNING_REF"
    - ./scripts/setup-remote-hsmd
    - source scripts/setup-env
    - cd lightning
    - bash -ex .github/scripts/setup.sh  # missing `set -e` internally
    - bash -ex .github/scripts/build.sh  # missing `set -e` internally

test-standalone:
  stage: test
  image: ubuntu
  variables:
    EXPERIMENTAL_FEATURES: "1"
    SUBDAEMON: hsmd:remote_hsmd
  script:
    - ./scripts/install-additional
    - ./scripts/maybe-rebase "$LIGHTNING_REF"
    - ./scripts/setup-remote-hsmd
    - source scripts/setup-env
    - cd lightning
    - bash -ex .github/scripts/setup.sh  # missing `set -e` internally
    - bash -ex .github/scripts/build.sh  # missing `set -e` internally

test-socket:
  variables:
    SUBDAEMON: hsmd:remote_hsmd_vls_grpc2
  stage: test
  image: ubuntu
  script:
    - ls -l
    - ls -l bin
    - ./scripts/install-additional
    - python3 --version
    - ./scripts/maybe-rebase "$LIGHTNING_REF"
    - ./scripts/setup-remote-hsmd
    - source scripts/setup-env
    - cd lightning
    - bash -ex .github/scripts/setup.sh  # missing `set -e` internally
    - export PYTEST_ADDOPTS="-ktest_pay"
    - bash -ex .github/scripts/build.sh  # missing `set -e` internally

