export PATH="$PATH:`pwd`/bin"
export PYTEST_PAR=10

# These are used by the remote_hsmd_inplace
export REMOTE_SIGNER_ALLOWLIST="$(pwd)/remote_hsmd/TESTING_ALLOWLIST"
export GREENLIGHT_VERSION=`cd lightning && git describe --always --dirty=-modded --abbrev=7`

# CLN integration tests need payment autoapproval (otherwise still enforcing)
export VLS_AUTOAPPROVE=1

# Force streaming of full blocks instead of using proofs (pretend all false-positive)
export VLS_CHAINFOLLOWER_TEST_STREAMING=1
